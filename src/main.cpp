#include <iostream>

#include "precision.hpp"
#include "buffer.hpp"

using namespace std;

#define HEMICUBE_GRID_SIZE 10

class Patch {
};

class Vertex {
  public:
    real x, y, z;
};

int main() {
  Buffer<int> b(10, 10);
  return 0;
}
